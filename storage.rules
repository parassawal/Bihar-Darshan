rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /stories/{userId}/{allPaths=**} {
      // Anyone can read images
      allow read: if true;
      // Users can only upload to their own specific folder
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
